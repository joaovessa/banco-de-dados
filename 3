CREATE TRIGGER tg_mensagem_novo_nome 
AFTER UPDATE ON Clientes
FOR EACH ROW
    SET old_nome = OLD.nome;
    SET new_nome = NEW.nome;
INSERT INTO Auditoria(mensagem)
VALUES ('nome atualizado de', old_nome, "para" , new_nome);
